// Placeholder file to keep TypeScript happy until Jest typings are configured.
// The actual CRA tests can live in *.test.js files; this one intentionally does nothing.

export {};

// Add a minimal test so Jest reports at least one test in this suite.
test('basic smoke: ts test runs', () => {
	expect(true).toBe(true);
});
